SuperSimpleCI
=============
**Author:** Malini Das

**Project:** SuperSimpleCI

**Requirements:** Posix, Python3+, Git

This is a simplified version of a distributed continuous integration (CI) system. 
It assumes you're running against a git repository, and needs git on the PATH.

def checker(server)
checke_r = threading.Thread(target=checker, args=(server,)) 创建监控线程
                                 ↑                  ↑
                       指定这个线程要执行的函数 访问 server 对象中的字段
checker.start() 启动监控线程

class ThreadingTCPServer(socketserver.ThreadingMixIn, socketserver.TCPServer):
同时继承:
    - ThreadingMixIn: 让每个请求在新线程中处理（并发）
    - TCPServer: 提供 TCP 服务器基础功能
server = ThreadingTCPServer((args.host, int(args.port)), DispatcherHandler)
#                             ↑                 ↑               ↑
#                          参数1: 地址      参数2: port      参数3: 处理器类
 
    |---->fun_c.start() 开始启动检查线程checke_r
__main__
    |---->server.serve_forever() 开始阻塞监听---->接受连接，创建新线程---->Handler 实例------>自动调用 handler.handle()--->执行业务逻辑（解析命令、处理注册/分发/结果）--->  handle() 返回，自动关闭连接，线程结束
                                                         |
                                                         ▼
                                                server.serve_forever()创建完 Handler 线程后立即返回,继续阻塞，accept() 等待下一个连接

┌─────────────┐       ┌────────────┐      ┌─────────────┐
│  Observer   │────▶ │ Dispatcher  │────▶│   Runner    │
│  (观察者)   │       │  (调度器)   │      │  (执行器)   │
└─────────────┘      └─────────────┘      └─────────────┘
      │                    │                    │
      ▼                    ▼                    ▼
  监控Git仓库          管理任务队列            运行测试
  检测是否有新提交     分配测试任务            执行测试
                      (发送runtest)              │
                           ▲                    │
                           │                    ▼
                           │              返回测试结果
                           │              (发送results)
                           │                    │
                           └────────────────────┘
                                        
                           ▼
                      保存结果到文件

┌─────────────────────────────────────────────────────────────────┐
│                        test_repo (主仓库)                        │
│                        开发者提交代码的地方                       │
│                                                                 │
│   开发者：git add . && git commit -m "fix bug"                   │
│                                                                 │
│   【用途】只有这一个仓库是"源头"，其他都是它的克隆                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ git clone (复制)
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ▼                   ▼                   ▼
   ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
   │   Observer   │    │   Runner 1   │    │   Runner 2   │
   │   的克隆      │    │   的克隆     │    │   的克隆      │
   └──────────────┘    └──────────────┘    └──────────────┘
          │                   │                   │
          │                   │                   │
          ▼                   ▼                   ▼
   每5秒 git pull       等待 Dispatcher      等待 Dispatcher
   对比 commit ID       分配任务               分配任务
   发现变化就通知       执行测试               执行测试

Files
=====
* repo_observer.py -- Checks the repo for changes and notifies the dispatcher
* dispatcher.py -- Receives test requests and dispatches them against test runners
* test_runner.py -- Runs the tests and returns the results 
* helpers.py -- Holds shared code
* update_repo.sh -- Updates the shared repo and drops a new file with the commit id if there's a change
* test_runner_script.sh -- Updates the test runner's repository to the given commit id 
* run_or_fail.sh -- Helper method used in update_repo.sh and test_runner_script.sh
* tests/ -- Holds some demo tests to run

Running the CI
==============

1. 先创建测试仓库
在项目目录外创建一个测试仓库（比如放在 /tmp 或你的工作目录）
cd /mnt/d/Vs_CodeSources/CC++/CI
mkdir test_repo
cd test_repo
git init

复制测试文件进去
cp -r ../code/tests ./
git add tests/
git commit -m "add tests"
2. 创建两个克隆供 CI 使用
回到 CI 项目目录
cd /mnt/d/Vs_CodeSources/CC++/CI

克隆给 observer 用
git clone test_repo test_repo_clone_obs

克隆给 test runner 用
git clone test_repo test_repo_clone_runner
3. 运行 CI 系统（需要 3 个终端窗口）
终端 1 - 启动 Dispatcher
cd /mnt/d/Vs_CodeSources/CC++/CI
python3 code/dispatcher.py

终端 2 - 启动 Test Runner
cd /mnt/d/Vs_CodeSources/CC++/CI
python3 code/test_runner.py --host localhost --port 8901 --dispatcher-server localhost:8888 ./test_repo_clone_runner
或
python3 code/test_runner.py ./test_repo_clone_runner

终端 3 - 启动 Repo Observer
cd /mnt/d/Vs_CodeSources/CC++/CI
chmod +x *.sh
python3 code/repo_observer.py --dispatcher-server=localhost:8888 ./test_repo_clone_obs

4. 触发一次测试
cd /mnt/d/Vs_CodeSources/CC++/CI/test_repo
touch new_file
git add new_file
git commit -m "new file"

# Resource
https://github.com/aosabook/500lines/tree/master/ci

Error Handling
==============

If you kill the test_runner.py process, dispatcher.py will figure out that
the runner is no longer available and will remove it from the pool.

You can also kill the test runner before it runs a test, to simulate a 
machine going down. If you do so, the dispatcher will realize the 
runner went down and will wait for a new runner to connect. Upon 
connection, it will dispatch the unfinished commit to it.

If you kill the dispatcher, the repo observer will figure out it went down
and will throw an exception. The test runners will also notice, and will
shut down.
